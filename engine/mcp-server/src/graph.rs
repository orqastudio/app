//! Artifact graph module — all graph operations are now delegated to the
//! orqa-daemon over HTTP. See `tools/graph.rs` for MCP tool implementations
//! and `daemon.rs` for the HTTP client.
//!
//! This module is retained for forward compatibility but contains no
//! active code.
