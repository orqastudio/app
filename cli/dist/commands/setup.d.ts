/**
 * Setup commands — dev environment bootstrapping.
 *
 * orqa setup link  — Install deps, build libs, and npm link everything
 */
/**
 * Dispatch the setup command: install deps, build libs, and npm link everything.
 * @param args - CLI arguments after "setup".
 */
export declare function runSetupCommand(args: string[]): Promise<void>;
//# sourceMappingURL=setup.d.ts.map