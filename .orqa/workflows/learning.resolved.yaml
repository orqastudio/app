# AUTO-GENERATED — do not edit manually.
# Stage: learn (learning-pipeline slot in agile-methodology)
# Source plugin: @orqastudio/plugin-core-framework v0.1.4-dev
# Generated at: 2026-03-29T09:05:07.492Z
version: 1.0.0
generated: true
generatedAt: 2026-03-29T09:05:07.492Z
description: Resolved learn stage workflow. Contains states, transitions, and artifact types contributed by
  @orqastudio/plugin-core-framework into the learning-pipeline slot of agile-methodology.
stage: learn
source_plugin: "@orqastudio/plugin-core-framework"
fills_slot: learning-pipeline
methodology: agile-methodology
contribution_workflow:
  name: learn-contribution
  initial_state: learn
  states:
    capture:
      category: completed
      description: Lesson capture — create lessons from implementation experience, review outcomes, and process observations.
      on_enter:
        - type: append_log
          params:
            message: Lesson capture started
    retrospective:
      category: completed
      description: Retrospective analysis — analyse captured lessons, identify what went well and what needs improvement.
      on_enter:
        - type: append_log
          params:
            message: Retrospective started
    pattern-tracking:
      category: completed
      description: Pattern tracking — aggregate lessons across epics, detect trends and recurring themes.
      on_enter:
        - type: append_log
          params:
            message: Pattern tracking started
    recurrence-detection:
      category: completed
      description: Recurrence detection — surface systemic issues that recur across multiple cycles, flag for process change.
      on_enter:
        - type: append_log
          params:
            message: Recurrence detection started
    learning_complete:
      category: completed
      description: Learning complete — lessons captured, patterns analysed, improvements identified.
      on_enter:
        - type: set_field
          params:
            field: learning_completed_at
            value: ${now}
        - type: append_log
          params:
            message: Learning complete
  transitions:
    - from: learn
      to: capture
      event: begin_learning
      description: Start the learning pipeline with lesson capture.
    - from: capture
      to: retrospective
      event: lessons_captured
      description: Lessons captured, begin retrospective analysis.
    - from: retrospective
      to: pattern-tracking
      event: retrospective_complete
      description: Retrospective done, begin pattern tracking across lessons.
    - from: pattern-tracking
      to: recurrence-detection
      event: patterns_identified
      description: Patterns identified, check for recurring systemic issues.
    - from: recurrence-detection
      to: learning_complete
      event: analysis_complete
      description: Recurrence analysis done, learning pipeline complete.
    - from: learning_complete
      to: done
      event: close_delivery
      description: Learning phase complete, close the delivery.
    - from: retrospective
      to: capture
      event: capture_more
      description: Retrospective revealed gaps — capture additional lessons.
    - from: pattern-tracking
      to: retrospective
      event: re_analyse
      description: Pattern tracking revealed insights requiring deeper retrospective analysis.
    - from: recurrence-detection
      to: pattern-tracking
      event: recheck_patterns
      description: Recurrence detection found new patterns — return to pattern tracking.
    - from: learning_complete
      to: capture
      event: reopen_learning
      description: New lessons surfaced — restart learning from capture.
artifact_types:
  decision:
    id_prefix: AD
    id_pattern: ^AD-[a-f0-9]{8}$
    default_path: .orqa/learning/decisions/
    icon: scale
    source_plugin: "@orqastudio/plugin-core-framework"
    fields:
      required:
        id:
          type: string
          pattern: ^AD-[a-f0-9]{8}$
      optional:
        title:
          type: string
        description:
          type:
            - string
            - "null"
        relationships:
          type: array
          items:
            type: object
            required:
              - target
              - type
            properties:
              target:
                type: string
              type:
                type: string
              rationale:
                type: string
    additional_properties: true
  rule:
    id_prefix: RULE
    id_pattern: ^RULE-[a-f0-9]{8}$
    default_path: .orqa/learning/rules/
    icon: shield
    source_plugin: "@orqastudio/plugin-core-framework"
    fields:
      required:
        id:
          type: string
          pattern: ^RULE-[a-f0-9]{8}$
        enforcement:
          type: array
      optional:
        title:
          type: string
        description:
          type:
            - string
            - "null"
        demoted_date:
          type: string
          format: date
          description: ISO date when the rule was demoted to inactive
        demoted_reason:
          type: string
          description: Why the rule was demoted (e.g. enforced by orqa lsp schema validation)
        replaced_by:
          type: string
          description: What now covers this rule (e.g. orqa lsp, pre-commit hook)
        stability_threshold:
          type: number
          minimum: 1
          default: 10
          description: Sessions with zero violations before the rule is safe to delete
        stability_count:
          type: number
          minimum: 0
          default: 0
          description: Current count of consecutive clean sessions since demotion
        relationships:
          type: array
          items:
            type: object
            required:
              - target
              - type
            properties:
              target:
                type: string
              type:
                type: string
              rationale:
                type: string
    additional_properties: true
  lesson:
    id_prefix: IMPL
    id_pattern: ^IMPL-[a-f0-9]{8}$
    default_path: .orqa/learning/lessons/
    icon: book-open
    source_plugin: "@orqastudio/plugin-core-framework"
    fields:
      required:
        id:
          type: string
          pattern: ^IMPL-[a-f0-9]{8}$
        recurrence:
          type: integer
          minimum: 0
      optional:
        title:
          type: string
        description:
          type:
            - string
            - "null"
        maturity:
          type: string
        relationships:
          type: array
          items:
            type: object
            required:
              - target
              - type
            properties:
              target:
                type: string
              type:
                type: string
              rationale:
                type: string
    additional_properties: true
  knowledge:
    id_prefix: KNOW
    id_pattern: ^KNOW-[a-f0-9]{8}$
    default_path: .orqa/documentation/knowledge/
    icon: zap
    source_plugin: "@orqastudio/plugin-core-framework"
    fields:
      required:
        id:
          type: string
          pattern: ^KNOW-[a-f0-9]{8}$
      optional:
        title:
          type: string
        description:
          type:
            - string
            - "null"
        thinking-mode:
          type: string
          description: Prompt classification mode this knowledge artifact represents. Used by the thinking-mode classifier to
            match prompts to behavioral context.
        priority-rules:
          type: array
          items:
            type: string
          description: Rule categories prioritised when this thinking mode is active. Used by the prompt injector to select
            relevant behavioral rules.
        relationships:
          type: array
          items:
            type: object
            required:
              - target
              - type
            properties:
              target:
                type: string
              type:
                type: string
              rationale:
                type: string
    additional_properties: true
  agent:
    id_prefix: AGENT
    id_pattern: ^AGENT-[a-f0-9]{8}$
    default_path: .claude/agents/
    icon: bot
    source_plugin: "@orqastudio/plugin-core-framework"
    fields:
      required:
        id:
          type: string
          pattern: ^AGENT-[a-f0-9]{8}$
        preamble:
          type: string
      optional:
        title:
          type: string
        description:
          type:
            - string
            - "null"
        model:
          type: string
        capabilities:
          type: array
          items:
            type: string
        knowledge:
          type: array
          items:
            type: string
        relationships:
          type: array
          items:
            type: object
            required:
              - target
              - type
            properties:
              target:
                type: string
              type:
                type: string
              rationale:
                type: string
    additional_properties: true
  principle-decision:
    id_prefix: PD
    id_pattern: ^PD-[a-f0-9]{8}$
    default_path: .orqa/learning/decisions/
    icon: scale
    source_plugin: "@orqastudio/plugin-core-framework"
    fields:
      required:
        id:
          type: string
          pattern: ^PD-[a-f0-9]{8}$
      optional:
        title:
          type: string
        description:
          type:
            - string
            - "null"
        relationships:
          type: array
          items:
            type: object
            required:
              - target
              - type
            properties:
              target:
                type: string
              type:
                type: string
              rationale:
                type: string
    additional_properties: true
  doc:
    id_prefix: DOC
    id_pattern: ^DOC-[a-f0-9]{8}$
    default_path: .orqa/documentation/
    icon: file-text
    source_plugin: "@orqastudio/plugin-core-framework"
    fields:
      required:
        id:
          type: string
          pattern: ^DOC-[a-f0-9]{8}$
      optional:
        title:
          type: string
        description:
          type:
            - string
            - "null"
        relationships:
          type: array
          items:
            type: object
            required:
              - target
              - type
            properties:
              target:
                type: string
              type:
                type: string
              rationale:
                type: string
    additional_properties: true
contribution_metadata:
  - plugin: "@orqastudio/plugin-core-framework"
    version: 0.1.4-dev
