---
id: TASK-2042c979
type: task
title: "Verify P2 One Context Window Per Task"
status: active
description: "Verify that each agent spawns fresh with a scoped prompt — no persistent agents or accumulated context"
relationships:
  - target: EPIC-2451d1a9
    type: delivers
    rationale: "Phase 4 — Principle Verification"
  - target: TASK-c9fa5dd0
    type: depends-on
    rationale: "Phase 3 (knowledge structure verified) must complete before principle verification"
acceptance:
  - "Audit report with PASS/FAIL verdict for P2"
  - "Agent spawning code reviewed and confirmed ephemeral — fresh context per task"
  - "No session-long agent state mechanisms found in codebase"
---

## What

Verify Principle 2 from RES-d6e8ab11 section 2:

> **P2: One Context Window Per Task** — Each agent spawns fresh for a single task with a precisely-scoped prompt. No persistent agents, no session-long lifecycles, no accumulated context. This eliminates stale agents, context drift, shutdown discipline problems, and the "context rot" documented in recent research (longer context degrades model reasoning quality, not just cost).

Confirm that the agent spawning mechanism creates a fresh agent per task with no state carried over between tasks.

## Knowledge Needed

- `.orqa/discovery/research/RES-d6e8ab11.md` section 2 (P2 definition)
- `libs/cli/src/lib/agent-spawner.ts` — agent spawning code
- `connectors/claude-code/` — connector agent management
- `.claude/agents/` — generated agent definitions

## Agent Role

Researcher — read-only audit producing a PASS/FAIL verdict with evidence.

## Steps

1. Read RES-d6e8ab11 section 2 to confirm the exact P2 principle text
2. Read `libs/cli/src/lib/agent-spawner.ts` to understand how agents are created
3. Search for any persistent agent mechanisms: session stores, agent pools, context accumulation between tasks
4. Search for any session-long lifecycle patterns: agent reuse, shared memory between spawns, persistent state files per agent
5. Verify that the connector delegates to fresh agents (no agent caching or persistence layer)
6. Review `.claude/agents/*.md` files to confirm they are task-scoped prompts, not session-scoped configurations
7. Document findings with file paths and evidence
8. Produce a PASS/FAIL verdict

## Verification

- `grep -rn "agent.*pool\|agent.*cache\|persistent.*agent\|session.*agent" libs/ connectors/ --include="*.ts" --include="*.rs"` — should return 0 matches
- `grep -rn "accumulated\|carry.*over\|shared.*context" libs/cli/src/lib/agent-spawner.ts` — should return 0 matches
- Agent spawner creates new instances per call — no singleton or reuse pattern
