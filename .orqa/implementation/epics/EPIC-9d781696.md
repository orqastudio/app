---
id: EPIC-9d781696
type: epic
title: "Cleanup — reconnect connector, verify migration, resolve transition artifacts"
description: "Final epic: reconnect the connector plugin to the new plugin-composed architecture, verify all migration is complete, remove transition-phase artifacts, and validate end-to-end."
status: active
priority: P1
created: 2026-03-25
updated: 2026-03-25
horizon: active
relationships:
  - target: MS-b1ac0a20
    type: fulfils
    rationale: "Cleanup completes the dogfooding milestone — system running on its own architecture"
  - target: EPIC-59b92c8d
    type: depends-on
    rationale: "Content migration must be complete before reconnecting"
---

## Scope

After all implementation and migration epics are complete:

### Reconnect Connector

- Re-enable the connector plugin in `.claude/settings.json`
- Verify the connector loads the new plugin-composed prompts (not the old monolithic ones)
- Verify hooks fire correctly with the new architecture
- Re-establish `.claude/` symlinks for rules, agents, knowledge as needed by the new system

### Verify Migration

- All 58 rules migrated to knowledge plugin artifacts with compressed summaries
- All agent definitions migrated to universal role templates + knowledge composition
- Prompt generation pipeline produces correct output for each role + stage + task combination
- Token budgets are within target ranges (1,500-4,000 per agent, 2,000-3,500 per orchestrator)
- State machines are plugin-owned and evaluating correctly

### Remove Transition Artifacts

- Remove the transition `.claude/CLAUDE.md` (replaced by connector-managed prompt)
- Remove specialized transition agents from `.claude/agents/` (replaced by universal roles + knowledge injection)
- Remove the PostToolUse hook on TaskUpdate (replaced by the prompt generation pipeline's re-grounding)
- Clean up `.state/` directory structure to match the new architecture

### Resolve Outstanding Items

- Close EPIC-7fd25812 (stabilisation) if still open
- Review all ideas captured during implementation for promotion
- Triage any lessons logged during the transition
- Run full graph validation and fix any integrity issues
- Run full test suite (`make check`) and fix any regressions

### Documentation

- Update `.orqa/documentation/` to reflect the new architecture
- Document the plugin-composed prompt system for future contributors
- Update the research document (RES-d6e8ab11) with actual implementation outcomes vs. predictions
