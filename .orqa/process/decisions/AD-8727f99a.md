---
id: AD-8727f99a
type: decision
title: "Rename tmp to .state for operational session data"
status: active
created: 2026-03-25
updated: 2026-03-25
description: "Rename the tmp/ directory to .state/ to reflect that session state and token metrics are operational state data, not disposable temporary files."
relationships:
  - target: RES-55bacef1
    type: informed-by
    rationale: "Comment on section 8 of the research identified the naming problem"
---

## Decision

Rename `tmp/` to `.state/` across the project. Session state, token metrics, and hook telemetry are operational state data scoped to a session — not disposable temporary files.

## Rationale

`tmp/` semantically means "safe to delete at any time." But `session-state.md` bridges context between sessions, `token-metrics.jsonl` tracks efficiency over time, and `hook-metrics.json` provides audit data. Naming the directory `tmp` invites accidental deletion and miscommunicates the purpose of its contents.

`.state/` communicates: "ephemeral per-session, but not disposable while a session is active."

## Consequences

- All references to `tmp/` in rules, hooks, scripts, and code must be updated
- `.gitignore` entry changes from `tmp/` to `.state/`
- Session state path becomes `.state/session-state.md`
- Token metrics path becomes `.state/token-metrics.jsonl`
- Team findings path becomes `.state/team/<name>/`
- Hook scripts that read/write `tmp/` must be updated
