---
id: TASK-96128eb2
type: task
name: "Verify prompt pipeline reads from .orqa/ only"
status: active
description: "Confirm runtime prompt generation reads exclusively from .orqa/prompt-registry.json, not plugins/ directly"
relationships:
  - target: EPIC-2451d1a9
    type: delivers
    rationale: "Phase 3 — Agent Prompt Generation Verification"
  - target: TASK-c298a900
    type: depends-on
    rationale: "Phase 1 complete — plugins restructured before verifying pipeline reads"
acceptance:
  - "Runtime prompt generation reads from .orqa/prompt-registry.json only"
  - "Install-time composition from plugins/ confirmed correct — this is the approved boundary"
  - "No runtime code path in daemon or connector reads plugins/ directly"
  - "Documentation of the install-time vs runtime boundary added as code comments where appropriate"
  - "npx tsc --noEmit passes for libs/cli"
---

# TASK-96128eb2: Verify prompt pipeline reads from .orqa/ only

## What to do

Confirm that the prompt generation pipeline respects the install-time vs runtime boundary:

1. **Verify runtime reads:**
   - Read `libs/cli/src/lib/prompt-registry.ts` — confirm it reads `.orqa/prompt-registry.json`
   - Read `libs/cli/src/lib/agent-file-generator.ts` — confirm it reads from the prompt registry output
   - Trace the call chain: where is the prompt registry loaded at runtime? Does it ever fall back to reading `plugins/`?

2. **Verify install-time composition:**
   - Confirm that `libs/cli/src/commands/install.ts` (or `cmdPluginSync`) calls the pipeline to compose from `plugins/` into `.orqa/`
   - This is architecturally correct per P7 (RES-d6e8ab11 section 3) — install-time reads `plugins/`, runtime reads `.orqa/`

3. **Check daemon and connector:**
   - Grep `libs/daemon/src/` for any import/read of `plugins/` directory
   - Grep `connectors/claude-code/src/` for any import/read of `plugins/` directory
   - These must be zero at runtime

4. **Document the boundary:**
   - If not already documented, add code comments at the key boundary points:
     - In the install pipeline: "Install-time: reads plugins/ to compose .orqa/"
     - In the runtime registry: "Runtime: reads .orqa/prompt-registry.json only"

## Knowledge needed

- `libs/cli/src/lib/prompt-registry.ts` — prompt registry implementation
- `libs/cli/src/lib/agent-file-generator.ts` — agent file generation
- `libs/cli/src/commands/install.ts` — install-time composition
- RES-d6e8ab11 section 3 — P7 composition model
- EPIC-2451d1a9 "Architecture Clarifications" section on install-time vs runtime

## Agent role

Researcher — reads code, verifies boundary, produces findings. Minor code comments if boundary is undocumented.

## Verification

- Grep `libs/daemon/src/` and `connectors/claude-code/src/` for `plugins/` — 0 runtime matches
- Confirm `prompt-registry.ts` references `.orqa/prompt-registry.json`
- Confirm `agent-file-generator.ts` uses registry output, not raw plugin dirs
- `npx tsc --noEmit` in `libs/cli/`
