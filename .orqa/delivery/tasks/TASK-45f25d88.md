---
id: TASK-45f25d88
type: task
name: "Verify token budget enforcement"
status: active
description: "Verify per-agent budgets, per-session budgets, priority trimming, and token tracking from RES-d6e8ab11 sections 6 and 8"
relationships:
  - target: EPIC-2451d1a9
    type: delivers
    rationale: "Phase 3 — Agent Prompt Generation Verification"
  - target: TASK-c298a900
    type: depends-on
    rationale: "Phase 1 complete — plugins restructured before verifying budget enforcement"
acceptance:
  - "Per-agent token budgets in prompt-pipeline.ts match the RES-d6e8ab11 section 6 table"
  - "Priority-based trimming implemented — P0 untouchable, P3 trimmed first, then P2, then P1"
  - "Per-session budget enforcement exists with configurable threshold ($5.00 default, 75%/90% warnings)"
  - "Token tracking writes to .state/token-metrics.jsonl with per-request, per-agent, per-session attribution"
  - "Budget defaults documented in code"
  - "npx tsc --noEmit passes for libs/cli"
---

# TASK-45f25d88: Verify token budget enforcement

## What to do

Verify the token budgeting system from RES-d6e8ab11 sections 6, 8, and 13:

1. **Per-agent budgets (section 6):**
   - Read `libs/cli/src/lib/prompt-pipeline.ts`
   - Find the `applyTokenBudget` function or equivalent
   - Verify budget values per role:
     - Orchestrator: 2,500 tokens
     - Implementer: 2,800 tokens
     - Reviewer: 1,900 tokens
     - Researcher: 2,100 tokens
     - Writer: 1,800 tokens
     - Designer: 1,800 tokens

2. **Priority-based trimming (section 8):**
   - Find the trimming logic in prompt-pipeline.ts
   - Verify trim order: P3 first, then P2, then P1
   - Verify P0 is never trimmed (untouchable)
   - Check that each knowledge/rule section has a priority level

3. **Per-session budget (section 13):**
   - Find session budget configuration
   - Verify $5.00 default
   - Verify 75% and 90% warning thresholds
   - Check how session cost is tracked

4. **Token tracking:**
   - Find token metrics capture — should write to `.state/token-metrics.jsonl`
   - Verify per-request attribution (which agent, which request)
   - Verify per-session attribution (cumulative session cost)
   - Check if hook-based capture exists (token usage from hook responses)

5. **Document findings:**
   - For each component: EXISTS and CORRECT / EXISTS but WRONG / MISSING
   - If MISSING, note what needs to be implemented

## Knowledge needed

- `libs/cli/src/lib/prompt-pipeline.ts` — token budget enforcement
- `libs/cli/src/lib/` — look for token tracking, session budget files
- `.state/` — token metrics output location
- RES-d6e8ab11 sections 6, 8, 13 — budget specifications

## Agent role

Researcher — reads code, verifies against spec, produces findings. No code changes.

## Verification

- Budget values compared against spec table — each role checked
- Trimming order verified in code: P3 -> P2 -> P1, P0 never
- Session budget and threshold values verified
- Token tracking output file path confirmed
- `npx tsc --noEmit` in `libs/cli/`
