---
id: "TASK-9021e959"
type: "task"
title: "Update Rust types: ArtifactEntry, scanner, ProjectSettings skill references"
description: "Update Rust source files to replace all 'skill' type references with 'knowledge' in ArtifactEntry, the artifact scanner, and ProjectSettings."
status: "ready"
created: 2026-03-20T00:00:00.000Z
updated: 2026-03-20T00:00:00.000Z
acceptance:
  - "ArtifactEntry type variant or string constant 'skill' renamed to 'knowledge'"
  - "Artifact scanner recognises 'knowledge' directory and KNOW- prefix"
  - "ProjectSettings skill-related fields renamed to knowledge equivalents"
  - "cargo clippy -- -D warnings passes with zero warnings"
  - "cargo test passes"
relationships:
  - target: "EPIC-fdcdb958"
    type: "delivers"
  - target: "TASK-30f5bdc8"
    type: "depends-on"
---

## What

Update Rust backend types and scanner to reflect the knowledge rename. The artifact scanner must recognise the new directory name and ID prefix. `ArtifactEntry` and `ProjectSettings` must use the new terminology.

## How

Search `backend/src-tauri/src/` for all references to `"skill"` (string literals), `Skill` (type names), and `SKILL` (prefix constants):
- `ArtifactEntry` enum or struct: rename variant/field
- Artifact scanner: update directory glob from `skills/` to `knowledge/`, update ID prefix check from `SKILL-` to `KNOW-`
- `ProjectSettings`: rename any `skills` field to `knowledge`
- Update all match arms and pattern matches that reference the old names
- Derive macros remain — just rename the string values

Run `make lint-backend` after changes to catch all clippy issues.

## Verification

1. `make lint-backend` passes (zero warnings)
2. `make test-rust` passes
3. `search_regex "SKILL-"` in Rust source returns zero matches
4. `search_regex "skills/"` in Rust source returns zero matches (outside comments/docs)