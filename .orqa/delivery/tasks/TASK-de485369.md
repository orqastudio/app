---
id: TASK-de485369
type: task
title: "Implement 4 graph query performance fixes"
description: "Implement all 4 fixes identified in graph query performance research: cache body in ArtifactNode, O(1) HashMap lookup for ID queries, daemon traceability endpoint, and server-side search filtering."
status: completed
created: 2026-03-24
updated: 2026-03-24
acceptance:
  - "ArtifactNode has body: Option<String> populated at graph-build time"
  - "query_artifacts() uses HashMap::get() for exact-ID lookups"
  - "Daemon has /traceability endpoint using cached graph"
  - "MCP graph_traceability proxies through daemon instead of rebuilding graph"
  - "Daemon /query endpoint accepts search parameter"
  - "MCP server no longer filters client-side"
  - "cargo check passes with zero warnings"
  - "All Rust tests pass (571 tests)"
relationships:
  - target: EPIC-c311d349
    type: delivers
    rationale: "Implementation task for the performance epic"
  - target: TASK-17f9e7d0
    type: depends-on
    rationale: "Research must complete before implementation"
---

## What

Implementation of all 4 fixes from the graph query performance research. Findings documented in `tmp/fix-graph-perf.md`.

## Files Modified

- `libs/validation/src/graph.rs` — body field on ArtifactNode
- `libs/validation/src/parse.rs` — cached body reads, O(1) lookup, search_filter param
- `libs/validation/src/daemon.rs` — /traceability endpoint, search param on /query
- `libs/validation/src/hooks.rs` — updated query_artifacts() call signature
- `libs/validation/src/bin/server.rs` — --search flag
- `libs/mcp-server/src/daemon.rs` — traceability() method on DaemonClient
- `libs/mcp-server/src/tools/graph.rs` — daemon proxy for traceability, server-side search
- `libs/mcp-server/src/server.rs` — updated tool dispatch
- `app/backend/src-tauri/src/commands/graph_commands.rs` — body: None in test nodes
- `app/backend/src-tauri/src/domain/status_transitions.rs` — body: None in test helper