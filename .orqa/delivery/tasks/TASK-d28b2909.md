---
id: TASK-d28b2909
type: task
title: "Document plugin relationship vocabulary as knowledge artifacts"
status: captured
priority: P1
created: 2026-03-25
updated: 2026-03-25
acceptance:
  - "Each plugin's relationship vocabulary documented as a knowledge artifact living with the plugin"
  - "agile-governance plugin has knowledge artifact explaining its 20 relationship types and their semantic distinctions"
  - "software plugin has knowledge artifact explaining its 10 relationship types and their semantic distinctions"
  - "Documentation page updated with canonical vocabulary reference"
  - "All stored inverse entries removed from .orqa/ artifact files (bulk cleanup after forward-only storage lands)"
relationships:
  - target: EPIC-4304bdcc
    type: delivers
    rationale: "Governance foundation work for stabilisation epic"
  - target: TASK-8c5f7004
    type: depends-on
    rationale: "Constraints must be correct before documenting the vocabulary"
---

## Context

Session 2026-03-25 research established:

1. **30 relationship types exist** across 2 plugins — all semantically relevant and unique
2. **agile-governance** owns 20 types (foundation, lineage, governance, knowledge-flow, agency, synchronisation)
3. **software** owns 10 types (hierarchy, dependency, corrective, delivery)
4. All other plugins correctly have empty relationship arrays
5. The vocabulary was never formally documented as knowledge artifacts

## What Changed from Previous Version

The previous version of this task (written by an earlier session) assumed:
- Only 10 types should exist (WRONG — all 30 are semantically valid)
- A "kill list" of types to remove (WRONG — never user-approved)
- Plugins don't declare relationships (WRONG — both plugins already declare them)

The corrected scope is documentation and cleanup, not vocabulary reduction.

## Canonical Locations

- Schema shape: `libs/types/schemas/orqa-plugin.json`
- TypeScript types: `libs/types/src/plugin.ts` (RelationshipType, RelationshipConstraints)
- Validation engine: `libs/validation/src/graph.rs`, `libs/validation/src/structural.rs`
- Plugin declarations: `plugins/agile-governance/orqa-plugin.json`, `plugins/software/orqa-plugin.json`
- Runtime discovery: `libs/validation/src/platform.rs` (scan_plugin_manifests)

## Dependency Chain

```
TASK-ff0a2460 (forward-only storage — remove MissingInverse validation)
  → TASK-8c5f7004 (fix from/to constraints, resolve merged-into duplication)
    → TASK-d28b2909 (document vocabulary, bulk-remove stored inverses)
```
