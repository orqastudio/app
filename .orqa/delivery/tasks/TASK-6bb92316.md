---
id: TASK-6bb92316
type: task
name: "Verify model tiering configuration"
status: active
description: "Verify agent-spawner.ts model tier defaults match the RES-d6e8ab11 section 4 tiering table"
relationships:
  - target: EPIC-2451d1a9
    type: delivers
    rationale: "Phase 3 — Agent Prompt Generation Verification"
  - target: TASK-c298a900
    type: depends-on
    rationale: "Phase 1 complete — plugin restructuring done before verifying model tiers"
acceptance:
  - "Model tier defaults exist in agent spawner code matching the RES-d6e8ab11 section 4 table"
  - "Orchestrator can override the default tier at delegation time"
  - "Model tier selection documented in code comments"
  - "npx tsc --noEmit passes for libs/cli"
---

# TASK-6bb92316: Verify model tiering configuration

## What to do

Verify that the agent spawner implements model tiering from RES-d6e8ab11 section 4:

1. **Read the agent spawner:**
   - Read `libs/cli/src/lib/agent-spawner.ts`
   - Find model tier defaults — look for model selection logic, role-to-model mapping

2. **Compare against the research table:**

   | Role | Recommended Model | Rationale |
| ------ | ------------------ | ----------- |
   | Orchestrator | Opus | Strongest reasoning for delegation decisions |
   | Planner | Opus | Architecture planning requires deep reasoning |
   | Implementer (complex) | Opus | Multi-file refactoring needs full capability |
   | Implementer (simple) | Sonnet | Straightforward single-file changes |
   | Reviewer | Sonnet | Pattern matching and checklist verification |
   | Researcher | Sonnet | Accuracy in reading, not creativity |
   | Writer | Sonnet | Documentation follows templates |
   | Designer | Sonnet | Component creation follows patterns |

3. **Check override mechanism:**
   - Can the orchestrator override the default model tier when delegating?
   - Is the override passed through the task delegation interface?
   - Is there a complexity assessment that switches Implementer between Opus/Sonnet?

4. **Check connector/integration coordination:**
   - How does the connector coordinate model selection with the agent spawner?
   - Is the model tier passed to the connector at spawn time?

5. **Document gaps:**
   - If defaults don't exist, document what needs to be added
   - If override mechanism is missing, document the design needed

## Knowledge needed

- `libs/cli/src/lib/agent-spawner.ts` — agent spawner implementation
- RES-d6e8ab11 section 4 — model tiering table and rationale
- `connectors/claude-code/` — connector agent spawning interface

## Agent role

Researcher — reads code, compares against research, produces findings. No code changes.

## Verification

- Agent spawner file read and model tier logic identified
- Each role in the table compared against code defaults
- Override mechanism presence confirmed or gap documented
- `npx tsc --noEmit` in `libs/cli/`
