---
id: EPIC-92de4797
type: epic
title: "Connector as thin adapter"
description: "Rewrite Claude Code connector hooks as thin I/O adapters that call the centralised Rust engine via CLI/daemon. Delete all local enforcement logic, frontmatter parsing, rule loading, schema scanning."
status: captured
priority: P1
relationships:
  - target: EPIC-81c336c1
    type: depends-on
    rationale: "Needs the Rust engine to have hook lifecycle and content loading"
  - target: EPIC-0f996fcb
    type: depends-on
    rationale: "Needs canonical hook types"
  - target: EPIC-79b90c42
    type: depends-on
    rationale: "Needs centralised logger"
  - target: MS-654badde
    type: fulfils
    rationale: "Epic fulfils this milestone"
---
# Connector as Thin Adapter

## Scope

- Create `src/hooks/adapter.ts` — shared I/O translation (stdin parse, canonical event mapping, output formatting)
- Rewrite each hook to: read stdin → translate to canonical → call daemon/CLI → translate result → output
- Hooks are TypeScript, compiled to `dist/hooks/`, referenced from `hooks.json`
- DELETE: bash-safety (redundant), schema-registry (use engine), telemetry (use logger), src/rule-engine.ts (TS duplicate)
- DELETE: all hooks/scripts/*.mjs (replaced by compiled TS)
- Connector-specific logic that STAYS: Claude Code systemMessage formatting, agent prompt structure, knowledge injection format, lesson→memory conversion