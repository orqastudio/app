---
id: EPIC-27c9606a
title: "App backend as daemon consumer"
description: "Refactor the Tauri app backend to connect to the daemon rather than embedding its own engine. Fix platform_config tests. Remove duplicated enforcement logic."
status: captured
priority: P2
relationships:
  - target: EPIC-81c336c1
    type: depends-on
    rationale: "Needs daemon running with HTTP API"
---
# App Backend as Daemon Consumer

- App startup ensures daemon is running (`orqa daemon start`)
- App connects to daemon HTTP API for all artifact operations
- Remove embedded platform_config that reads core.json (use daemon)
- Fix 7 test failures (use plugin manifests via daemon, not empty core.json)
- Remove any duplicated enforcement/validation logic
- SDK provides TypeScript-to-Rust bridge via Tauri invoke → daemon
