---
id: DOC-9e3c5073
type: doc
title: Three-Layer Enforcement Model
description: "How OrqaStudio enforces governance through three layers: LSP real-time diagnostics for mechanical checks, behavioral rules for judgement calls, and pre-commit hard gates as the final safety net. Includes the demotion model for transitioning rules between layers."
category: governance
created: 2026-03-24
updated: 2026-03-24
relationships:
  - target: KNOW-9e3c5072
    type: synchronised-with
    rationale: "Agent-facing knowledge pair for this user-facing documentation page"
---

# Three-Layer Enforcement Model

## Overview

OrqaStudio enforces governance through three layers, each targeting a different class of violation. The layers work together: mechanical checks catch deterministic errors instantly, behavioral rules guide agent judgement, and pre-commit gates ensure nothing slips through.

## The Three Layers

### Layer 1: LSP Real-Time Diagnostics

The fastest enforcement layer. The LSP mode (`orqa lsp`) validates artifact files in real-time as you edit them in your IDE.

**What it catches:**

| Violation | LSP Response |
|-----------|-------------|
| Invalid status value (e.g., `enabled` instead of `active`) | Red squiggle + suggested valid values |
| Wrong relationship type (e.g., `synced-with` instead of `synchronised-with`) | Red squiggle + valid type list |
| Missing required field (e.g., no `id` in frontmatter) | Warning squiggle |
| Broken artifact reference (e.g., `target: KNOW-999999`) | Error squiggle |
| Type mismatch (e.g., `created: true` instead of a date) | Error squiggle |

**Characteristics:**
- Mechanical and deterministic — the same input always produces the same diagnostic
- Driven by plugin `schema.json` files — no hardcoded rules
- Zero latency feedback — errors appear as you type
- Uses the shared validation engine (`libs/validation/`)

### Layer 2: Behavioral Rules (Prompt Injection)

The judgement layer. Some governance constraints cannot be reduced to a schema check — they require understanding context, intent, and trade-offs. These are enforced by injecting rules into agent context at delegation time.

**What it catches:**

| Violation | Enforcement |
|-----------|------------|
| Documentation-before-code | Orchestrator includes the rule in delegation prompt |
| Delegation boundary crossing | Role constraints in agent system prompt |
| Pillar misalignment (feature doesn't serve a pillar) | Pillar gate questions in context |
| Process sequencing (skipping steps) | Workflow rules in context |
| Incomplete reporting (missing "What Is NOT Done") | Output structure requirements in context |

**Characteristics:**
- Requires judgement — the agent must interpret and apply the rule
- Context-dependent — the same rule may apply differently in different situations
- Enforced via prompt injection by the orchestrator and connector hooks
- Cannot be replaced by mechanical checks (deterministic tools cannot make judgement calls)

### Layer 3: Pre-Commit Hard Gate

The final safety net. Nothing enters the repository without passing all checks.

**What it catches:**

| Check | Tool |
|-------|------|
| Artifact schema validation | Shared validation engine (same as LSP) |
| Rust lint violations | `cargo clippy` with pedantic warnings |
| TypeScript/Svelte type errors | `svelte-check`, ESLint |
| Test failures | `cargo test`, Vitest |
| Formatting violations | `cargo fmt --check` |

**Characteristics:**
- Hard gate — commit is blocked if any check fails
- Runs `orqa check` which calls the shared validation engine plus language-specific tools
- Redundant with LSP for schema checks (defense in depth)
- Logs violations to `tmp/precommit-violations.jsonl` for the stability tracker

## How the Layers Work Together

```
You edit an artifact file
        │
        ▼
  Layer 1: LSP
  Red squiggle appears immediately
  You fix it before saving
        │
        ▼
  Layer 2: Behavioral Rules
  Agent follows injected rules during work
  Orchestrator verifies process compliance
        │
        ▼
  Layer 3: Pre-Commit
  Final check on git commit
  Blocks if anything slipped through
```

In practice:
- **Layer 1 catches most mechanical errors** before they are ever committed
- **Layer 2 catches process violations** that require judgement
- **Layer 3 catches anything that escaped** Layers 1 and 2

## Rule Demotion Model

When a behavioral rule (Layer 2) becomes mechanically enforceable by the LSP or pre-commit hook, it can be **demoted** from active to inactive. This prevents rule duplication — a mechanical check is always stronger than a behavioral reminder.

### Demotion Flow

```
Behavioral rule (Layer 2, active)
        │
  Mechanical enforcement added (Layer 1 or 3)?
        │ YES
        ▼
  Demote rule to inactive
  Set demoted_date, demoted_reason, replaced_by
        │
        ▼
  Stability tracking begins
  (see DOC-2330c9f6 for the full lifecycle)
        │
        ▼
  After N clean sessions → Safe to delete
```

### What CAN Be Demoted

Rules that enforce constraints the schema and validation engine can check:
- Valid status values
- Valid relationship types
- Required frontmatter fields
- Artifact ID format
- File naming conventions

### What CANNOT Be Demoted

Rules that require human or agent judgement:
- Pillar alignment (is this feature serving the product vision?)
- Documentation-before-code (is the docs update genuine, not a stub?)
- Honest reporting (is the completion report accurate?)
- Delegation boundaries (is the orchestrator staying in its lane?)
- Process sequencing (are steps being followed in order?)

## Enforcement Health

The system's enforcement health can be assessed by looking at coverage across all three layers:

| Health Indicator | Meaning |
|-----------------|---------|
| All mechanical rules have LSP diagnostics | Layer 1 is complete |
| All judgement rules are in the prompt injection set | Layer 2 is complete |
| Pre-commit runs the validation engine | Layer 3 is complete |
| Demoted rules are being tracked | Demotion pipeline is working |
| No enforcement gaps (rules without any layer) | Full coverage |

## Related Documents

- [KNOW-9e3c5072](KNOW-9e3c5072) — Agent-facing knowledge pair for this documentation page
- [DOC-8d2b4f62](DOC-8d2b4f62) — Shared Validation Engine (the engine powering Layers 1 and 3)
- [DOC-2330c9f6](DOC-2330c9f6) — Demoted Rule Stability Tracking (the lifecycle after demotion)
