// Lesson domain types and parse/render logic — re-exported from orqa-engine.
//
// Lesson, NewLesson, parse_lesson, and render_lesson are all defined in the
// engine crate so they are available to all access layers.

pub use orqa_engine::lesson::*;
