// Project settings types — re-exported from orqa-engine.
//
// All project settings types (ProjectSettings, StatusDefinition, PluginProjectConfig, etc.)
// are defined in the engine crate so they are available to all access layers.

pub use orqa_engine::project::*;
