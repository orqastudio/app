// Search and embedding model configuration.
// Centralises model names used in UI copy so they stay in sync with
// the sidecar's model selection without scattering string literals.

/** The embedding model used for semantic search in the sidecar. */
export const EMBEDDING_MODEL_NAME = "all-MiniLM-L6-v2";
