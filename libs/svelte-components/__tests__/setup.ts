// Global test setup for @orqastudio/svelte-components.
// Extends vitest's expect with jest-dom matchers (toBeInTheDocument, etc.)
// Must use the /vitest entrypoint to ensure expect is imported from vitest.
import "@testing-library/jest-dom/vitest";
