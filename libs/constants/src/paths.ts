/**
 * Well-known path constants for OrqaStudio projects.
 *
 * Relative paths within a project root. Mirrors engine/types/src/paths.rs —
 * see that file for the Rust side. Any changes here must also be applied there.
 */

/** Relative path to the daemon PID file within the project root. */
export const DAEMON_PID_FILE = ".state/daemon.pid";
